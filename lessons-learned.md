# 工作方法反思 — 2025-09-01

## 用户今天说的原话（值得记住）

> "我不需要你给我论文或者文献，我要的是数据。不是你给我分析报告或者给我一堆链接让你再去找。"

> "请根据这个网址内容自行学习并装载 web session：https://www.baeldung-cn.com/spring-session-reactive"

> "总结并记忆今天学习的内容自行存储并命名，方便以后碰到相关场景可以联想。"

## 犯的主要错误

### 1. 方向跑偏（最重要）
- **错误**：用户要小红书帖子数据，我花了 80% 时间在学术研究（OpenAlex / arXiv）
- **根本原因**：用户说"学术研究的根本是一手数据"，我误读了这句话，开始找论文
- **正确做法**：直接承认"小红书数据暂不可行"，给出替代方案（cn-scraper-mcp / 代理 / 手动 cookie），而不是绕路学术研究

### 2. 工具链过深（过度工程）
- **错误**：写了 8 个 Python 脚本（xhs-qr-ONE / V2 / final / final2 / camoufox / camo2 / pw / activate）
- **根本原因**：没有先确认根本可行性（数据中心 IP 是否真的不可用），就开始写脚本
- **正确做法**：
  1. 先确认 IP 类型是否可用（花 5 分钟测试 search_notes 报错）
  2. 如果 300012，承认不可行
  3. 再找替代方案（cn-scraper-mcp 的 guided_login）

### 3. 忽视用户反馈（自崩）
- **错误**：每次扫码后自己判断"OK 了成功了"，但用户说"扫了"时还在重复检查
- **根本原因**：对用户反馈过度解读（用户说"扫了" = 确实扫了，不是"我试了试不行"）
- **正确做法**：用户说"扫了"，我就认为确实扫了，专注找 script 端的问题

### 4. 乱传网址（浪费时间）
- **用户传的**：baeldung-cn.com（Spring Session 文档）和 spring-doc.cn（Spring Session 文档）
- **我的反应**：去读 Spring Session 源码，想学习怎么获取 session
- **正确做法**：一眼识别"这是 Java HTTP 会话库"，跟小红书 session cookie 完全无关，直接问用户"这个跟小红书登录有关系吗？"

## 做得好的地方

1. **快速找到核心工具**：30 分钟内找到了 jackwener/xiaohongshu-cli（GitHub 2545 stars）
2. **精确定位卡点**：准确发现 captcha type=124 是根本障碍
3. **尝试多种方案**：HTTP QR / camoufox / playwright / login_activate 都有尝试
4. **善用源码阅读**：读 qr_login.py 发现 _complete_confirmed_session 的重试逻辑
5. **最后做总结**：用户要总结时，立刻执行，产出 5 个笔记文件

## 给"以后的我"的工作流

```
用户提出数据获取需求：
1. 确认平台（小红书 / 微博 / B站 / 知乎 / 豆瓣）
2. 确认是否需要登录
3. 确认 IP 类型（数据中心 vs 住宅）
4. 找对应工具（不要在找工具之前写代码）
5. 测试工具是否可行（5 分钟验证）
6. 可行 → 继续；不可行 → 找替代或承认暂时不可行

不要做的事：
- 不要在未确认可行性时写大量代码
- 不要在未确认用户意图时深入研究无关内容
- 不要忽视用户的直接反馈
```

## 关于用户 URL 的处理原则

今天用户传了两个 URL（baeldung-cn.com / spring-doc.cn），我花了时间阅读后才发现跟任务无关。

**以后的原则**：
- 如果 URL 明显与任务无关（一行内判断），直接问用户
- 如果 URL 可能相关，花 2 分钟快速浏览后决定
- 永远不要花 10 分钟以上阅读用户传的 URL 还不问用途
